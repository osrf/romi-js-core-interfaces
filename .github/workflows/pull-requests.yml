name: Pull Requests
on: pull_request

jobs:
  unit-test:
    name: Unit Test
    runs-on: ubuntu-latest
    env:
      CI: true
    steps:
      - uses: actions/checkout@v2
      - name: unit test
        uses: ./.github/actions/unit-test
